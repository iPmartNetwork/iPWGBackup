# iPWGBackup

**WireGuard Telegram Backup** – بکاپ خودکار و دستی سرور WireGuard به تلگرام.

---

## ویژگی‌ها

- 💻 **بکاپ خودکار هر 12 ساعت:** توسط systemd timer فایل TAR تولید و به تلگرام ارسال می‌شود.  
- 🤖 **بکاپ دستی از طریق Telegram:** کاربران با ارسال دستور `/backup` در ربات تلگرام می‌توانند بکاپ فوری دریافت کنند.  
- 📦 **خروجی TAR بدون رمزگذاری:** ساده و قابل استخراج در تمام سیستم‌ها.  
- 👥 **پشتیبانی از چند Chat ID:** چند Chat ID با کاما جدا کنید تا بکاپ برای همه ارسال شود.  
- 📄 **لاگ کامل:** عملیات بکاپ در `/var/log/wg_backup.log` ذخیره می‌شود.  
- 🔧 **نصب آسان:** اسکریپت Bash تمام پیش‌نیازها، سرویس‌ها و تایمرها را مدیریت می‌کند.  
- 🔄 **بات تلگرام دائم:** همیشه آماده دریافت دستور `/backup` حتی پس از ریبوت سرور.  

---

## پیش‌نیازها

- سرور Linux با دسترسی root  
- Python 3 و pip  
- Telegram Bot Token و Chat ID  

---

## نصب

1. دانلود و اجرای اسکریپت نصب:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/iPmartNetwork/iPWGBackup/master/install-wg-backup.sh)

```

هنگام نصب، از شما درخواست می‌شود:

Telegram Bot Token

Telegram Chat ID(s) (چند ID با کاما جدا شوند)

پس از نصب:

بات تلگرام دائمی راه‌اندازی می‌شود و آماده دریافت /backup است.

بکاپ خودکار هر 12 ساعت به تلگرام ارسال می‌شود.



فایل TAR هر 12 ساعت به Chat IDهای تعیین‌شده ارسال می‌شود.

بکاپ دستی

به ربات تلگرام پیام /backup ارسال کنید → بکاپ فوری برای همان چت ارسال می‌شود.


مدیریت سرویس‌ها

بررسی وضعیت بات تلگرام:

```bash
systemctl status wg-backup-bot.service
```

بررسی وضعیت بکاپ خودکار:

```bash
systemctl status wg-backup.timer
```

شروع/توقف دستی بات تلگرام:

```bash
systemctl start|stop wg-backup-bot.service
```

شروع/توقف دستی بکاپ خودکار:

systemctl start|stop wg-backup.timer
